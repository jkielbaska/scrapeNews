// aktualna godzina
// liczba artykułów wyświetlanych w głównym contencie

export const Footer = () => {
  //wyświetl aktualną datę po prawej stronie footera w formacie dd.mm.yyyy

  return (
    <div>
      <footer className="mt-auto bg-gray-900 text-white p-4 relative">
        <p>footer</p>
      </footer>
    </div>
  );
};
